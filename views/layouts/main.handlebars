<!DOCTYPE html>
<html lang="en">

<head>
	<meta charset="UTF-8">
    <link rel="stylesheet" href="/stylesheets/style.css">
	<script type="text/javascript" src="https://unpkg.com/webcam-easy/dist/webcam-easy.min.js"></script>
	<title>Table Top Warriors</title>
</head>

<body>
	<div>
		<header class="header">
			<h1>
				<image src="/images/TTW-Header-Logo.png" alt="Table Top Warriors Logo 2" onclick=location.href='/' />
			</h1>
			<nav class="header-nav">
				{{#if loggedIn}}
				{{!-- If user is logged in display logout button --}}
				<a class="login-signup-btn" href="/dashboard">Dashboard</a>
				<a href="/" id="logout-btn">Logout</a>
				{{else}}
				{{!-- If user is not logged in display link to login page --}}
				<button onclick=location.href='/login' class="login-signup-btn">Log In / Sign Up</button>
				{{/if}}
			</nav>
		</header>

		<main>
			{{{ body }}}
		</main>
		<footer>
			Thanks for visiting!
		</footer>
	{{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
	</div>
</body>

</html>